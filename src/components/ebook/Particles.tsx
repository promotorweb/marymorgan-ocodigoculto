import { useMemo } from "react";export function Particles({count=18}:{count?:number}){const items=useMemo(()=>Array.from({length:count}).map(()=>({left:Math.random()*100,size:1+Math.random()*2.5,delay:Math.random()*20,duration:18+Math.random()*22,bottom:-10-Math.random()*30})),[count]);return <div className="pointer-events-none absolute inset-0 overflow-hidden no-print">{items.map((p,i)=><span key={i} className="particle" style={{left:`${p.left}%`,bottom:`${p.bottom}%`,width:`${p.size}px`,height:`${p.size}px`,animationDelay:`${p.delay}s`,animationDuration:`${p.duration}s`}} />)}</div>}